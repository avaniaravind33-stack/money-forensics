services:
  - type: web
    name: money-forensics-backend
    env: python
    buildCommand: pip install fastapi uvicorn pydantic python-dotenv
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEMO_MODE
        value: "true"
